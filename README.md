# Filmorate 🎬
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
Социальная сеть для оценки и обсуждения фильмов. Платформа позволяет пользователям делиться своими впечатлениями о фильмах, ставить оценки, находить друзей и открывать для себя новые кинокартины.

## 📋 Содержание
### Функциональность

### Технологии

### База данных

### API

### Установка и запуск

## Примеры использования

### 🚀 Функциональность
#### Пользователи
✅ Регистрация и управление профилем пользователя

✅ Поиск и добавление в друзья

✅ Просмотр списка друзей и общих друзей

✅ Удаление из друзей

#### Фильмы
✅ Добавление и редактирование информации о фильмах

✅ Система лайков (оценок)

✅ Рейтинг популярных фильмов

✅ Фильтрация по жанрам и рейтингам MPA

✅ Поиск фильмов по различным критериям

#### Дополнительные возможности
✅ Управление жанрами фильмов

✅ Система рейтингов MPA

✅ Валидация данных

✅ Логирование операций

✅ Обработка ошибок

### 🛠 Технологии
Backend
Java 17 - основной язык программирования

Spring Boot - фреймворк для создания приложения

Spring MVC - архитектура веб-приложения

Lombok - уменьшение boilerplate кода

Maven - управление зависимостями

### База данных
H2 / PostgreSQL - реляционная база данных

JDBC - работа с базой данных

### Валидация
Jakarta Validation - валидация входных данных

Custom Validators - кастомные правила валидации

### Документация
OpenAPI 3.0 - документация API

Swagger UI - интерактивная документация

### 🗄 База данных
#### Схема базы данных
![](DB_filmorate.png)

### Основные таблицы
#### users - Пользователи
CREATE TABLE users (
user_id BIGINT AUTO_INCREMENT PRIMARY KEY,
email VARCHAR(255) NOT NULL UNIQUE,
login VARCHAR(255) NOT NULL UNIQUE,
name VARCHAR(255),
birthday DATE NOT NULL
);
#### film - Фильмы
CREATE TABLE IF NOT EXISTS films (
id BIGINT AUTO_INCREMENT PRIMARY KEY,
name VARCHAR(255) NOT NULL,
description VARCHAR(255) NOT NULL,
release_date DATE,
duration INTEGER,
rating_id INTEGER,
FOREIGN KEY (rating_id) REFERENCES rating(id)
);
#### friendships - Дружба между пользователями
CREATE TABLE IF NOT EXISTS FRIENDSHIPS (
friendship_id BIGINT AUTO_INCREMENT PRIMARY KEY,
user_id BIGINT NOT NULL,
friend_id BIGINT NOT NULL,
FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
FOREIGN KEY (friend_id) REFERENCES users(id) ON DELETE CASCADE
);
#### likes - Лайки фильмов
CREATE TABLE IF NOT EXISTS likes (
id INTEGER NOT NULL AUTO_INCREMENT PRIMARY KEY,
user_id BIGINT NOT NULL,
film_id BIGINT NOT NULL,
FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
FOREIGN KEY (film_id) REFERENCES films(id) ON DELETE CASCADE
);

#### film_genre - Связь фильмов и жанров
CREATE TABLE IF NOT EXISTS film_genres (
id integer AUTO_INCREMENT PRIMARY KEY,
film_id BIGINT NOT NULL,
genre_id BIGINT NOT NULL,
FOREIGN KEY (film_id) REFERENCES films(id) ON DELETE CASCADE,
FOREIGN KEY (genre_id) REFERENCES genres(id) ON DELETE CASCADE
);
#### mpa_rating - Рейтинги MPA
CREATE TABLE IF NOT EXISTS rating (
id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
name VARCHAR(255) NOT NULL
);
#### genre - Жанры фильмов
CREATE TABLE IF NOT EXISTS genres (
id INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
name VARCHAR(255) NOT NULL
);
## 🌐 API
### Базовый URL
http://localhost:8080
#### Основные эндпоинты
#### Пользователи
GET /users - получить всех пользователей

GET /users/{id} - получить пользователя по ID

POST /users - создать нового пользователя

PUT /users - обновить пользователя

DELETE /users/{id} - удалить пользователя
#### Друзья
PUT /users/{userId}/friends/{friendId} - добавить друга

DELETE /users/{userId}/friends/{friendId} - удалить из друзей

GET /users/{id}/friends - получить список друзей

GET /users/{id}/friends/common/{otherId} - получить общих друзей
#### Фильмы
GET /films - получить все фильмы

GET /films/{id} - получить фильм по ID

POST /films - добавить новый фильм

PUT /films - обновить фильм

GET /films/popular - получить популярные фильмы
#### Лайки
PUT /films/{id}/like/{userId} - поставить лайк

DELETE /films/{filmId}/like/{userId} - удалить лайк
#### Жанры и рейтинги
GET /genres - получить все жанры

GET /genres/{id} - получить жанр по ID

GET /mpa - получить все рейтинги MPA

GET /mpa/{id} - получить рейтинг MPA по ID
### Примеры запросов
#### Создание пользователя
POST /users
Content-Type: application/json

{
"email": "user@example.com",
"login": "user_login",
"name": "Иван Иванов",
"birthday": "1990-01-01"
}
#### Добавление фильма
POST /films
Content-Type: application/json

{
"name": "Терминатор",
"description": "Фантастический боевик о киборге-убийце",
"releaseDate": "1984-10-26",
"duration": 107,
"mpa": {"id": 4}
}
## 🚀 Установка и запуск
### Предварительные требования
Java 17 или выше

Maven 3.6+

База данных (H2/PostgreSQL)
### Шаги установки
#### Клонирование репозитория
git clone https://github.com/Borgex14/java-filmorate.git
cd java-filmorate
#### Настройка базы данных
Для H2 (встроенная база)
Настройки в application.properties

Для PostgreSQL
Создайте базу данных и обновите настройки подключения
#### Сборка приложения
mvn clean package
#### Запуск приложения
java -jar target/filmorate-1.0.0.jar
#### Проверка работоспособности
curl http://localhost:8080/users
### Конфигурация
Основные настройки в application.properties:
#### Сервер
server.port=8080

#### База данных
spring.datasource.url=jdbc:h2:mem:filmorate
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

#### H2 Console (для разработки)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

#### Логирование
logging.level.ru.yandex.practicum.filmorate=DEBUG
## 💡 Примеры использования
### Получение топ N популярных фильмов
SELECT f.*, COUNT(l.user_id) AS like_count
FROM film f
LEFT JOIN likes l ON f.film_id = l.film_id
GROUP BY f.film_id
ORDER BY like_count DESC
LIMIT 10;
### Получение общих друзей
SELECT u.*
FROM users u
JOIN friendships f1 ON u.user_id = f1.friend_id
JOIN friendships f2 ON u.user_id = f2.friend_id
WHERE f1.user_id = ? AND f2.user_id = ?
AND f1.status = 'CONFIRMED' AND f2.status = 'CONFIRMED';
### Получение рекомендованных фильмов
SELECT DISTINCT f.*
FROM film f
JOIN likes l ON f.film_id = l.film_id
JOIN likes l2 ON l.user_id = l2.user_id
WHERE l2.film_id IN (
SELECT film_id FROM likes WHERE user_id = ?
)
AND f.film_id NOT IN (
SELECT film_id FROM likes WHERE user_id = ?
)
ORDER BY (
SELECT COUNT(*) FROM likes WHERE film_id = f.film_id
) DESC;
## 🧪 Тестирование
#### Запуск тестов
mvn test
#### Интеграционные тесты
mvn verify
#### Проверка покрытия кода
mvn jacoco:report
## 📊 Мониторинг и логирование
### Приложение использует многоуровневое логирование:

INFO - основные операции

DEBUG - детальная информация для разработки

WARN - предупреждения

ERROR - ошибки приложения
## 🔮 Планы по развитию
#### Добавить систему рекомендаций на основе машинного обучения

#### Реализовать полнотекстовый поиск по фильмам

#### Добавить модерацию контента

#### Реализовать уведомления о новых фильмах

#### Добавить мобильное приложение

#### Интеграция с социальными сетями
## 🤝 Участие в разработке
#### Форкните репозиторий

#### Создайте feature branch (git checkout -b feature/amazing-feature)

#### Закоммитьте изменения (git commit -m 'Add some amazing feature')

#### Запушьте branch (git push origin feature/amazing-feature)

#### Откройте Pull Request
## 📄 Лицензия
#### Этот проект лицензирован под MIT License - смотрите файл [LICENSE](./LICENSE) для деталей.
## 👥 Команда разработки
#### Разработано в рамках учебного проекта Яндекс.Практикум.